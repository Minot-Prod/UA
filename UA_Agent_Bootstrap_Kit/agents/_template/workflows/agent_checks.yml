name: Agent Template Checks
on:
  pull_request:
    paths:
      - "agents/**"
jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo
        run: echo "Template OK"
