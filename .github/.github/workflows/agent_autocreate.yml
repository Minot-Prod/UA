name: Agent Autocreate

on:
  workflow_dispatch:
  repository_dispatch:
    types: [ua_agent_request]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Autocreate workflow placeholder"
