
diff --git a/.github/workflows/ua_ci_check.yml b/.github/workflows/ua_ci_check.yml
index 0000000..2222222 100644
--- a/.github/workflows/ua_ci_check.yml
+++ b/.github/workflows/ua_ci_check.yml
@@ -10,7 +10,7 @@ jobs:
     steps:
       - name: Checkout repository
         uses: actions/checkout@v4
-      - name: Setup Node.js
-        uses: actions/setup-node@v4
-        with:
-          node-version: 22
+      - name: Setup Node.js (use LTS 20)
+        uses: actions/setup-node@v4
+        with:
+          node-version: 20

       - name: Install dependencies (with cache)
         run: |
           npm ci || npm install
+
+      - name: Safe Netlify check (if configured)
+        run: |
+          if [ -n "$PARLIOS_SITE_ID" ]; then
+            echo "⚙️ Running Netlify CLI safely..."
+            npx netlify --telemetry-disable --json || echo "⚠️ Netlify CLI exited non-zero, continuing safely"
+          else
+            echo "ℹ️ No Netlify site configured — skipping Netlify check."
+          fi
