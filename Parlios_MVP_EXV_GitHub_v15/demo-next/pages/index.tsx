export default function Home(){
  return (<main style={{padding:'1rem'}}>
    <h1>Parlios — Observabilité</h1>
    <p>Déclenche quelques clics, puis ouvre <code>/api/telemetry/export</code> pour voir les événements (démo mémoire).</p>
    <p>Note: rien n'est envoyé si le consentement <strong>Analytics</strong> n'est pas actif (Étape 10).</p>
  </main>)
}
